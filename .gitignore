# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Certs (mkcert output — regenerate locally, never commit)
certs/

# Runtime state
server/session.json
server/tools/*.log
*.log
server/data/prefs/
server/data/gestures/
server/data/models/*.pkl
server/data/decisions/
server/data/shows/
freesolo/*/dataset/

# Firmware secrets
firmware/wand/config.h

# UNO Q stream-probe: generated per-deployment (ws_url/session), never commit
firmware/uno_q/stream_probe/python/probe_config.json

# Retro Diffusion API token (secret — never commit; read by server/tools/gen_assets.py)
server/tools/rd_token.txt
.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
songs/
server/data/last_song.*
scratchpad/
